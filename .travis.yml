language: node_js
node_js:
  - "node"
addons:
    postgresql: "9.6"
services:
  - postgresql
before_script:
  - psql -c 'CREATE DATABASE gutsy_test;' -U postgres
script:
  - yarn test
  - yarn lint
after_script: yarn run test-coverage
